@Library("jenkins shared lib@main") _

pipeline {
    agent { label 'alpine'}
    tools {
        maven 'maven 3.9.0'
    }
    stages {
        stage('Maven BUILD') {
            steps {
                mavenBuild()
            }
        }
    }
}
